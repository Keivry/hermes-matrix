FROM nousresearch/hermes-agent:latest

RUN apt-get update && apt-get install -y libolm-dev && rm -rf /var/lib/apt/lists/*

RUN . /opt/hermes/.venv/bin/activate && uv pip install --no-cache-dir 'hermes-agent[matrix]'
